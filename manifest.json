{
  "name": "google-places",
  "description": "Search for places and get details like ratings, opening hours, and reviews using Google Places.",
  "config": {
    "api_key": {
      "description": "Google API key with Places API (New) enabled.",
      "required": true
    }
  }
}
